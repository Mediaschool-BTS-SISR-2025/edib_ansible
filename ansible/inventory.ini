[vmhosts]
# Example local testing entry (keep for local tests)
# localhost ansible_connection=local

# Remote server (école) configured from your SSH command:
# ssh -i ./.ssh/mediaschool edib@37.64.159.66 -p 2222
37.64.159.66 ansible_user=edib ansible_port=2222 ansible_ssh_private_key_file=~/.ssh/mediaschool ansible_connection=ssh

# Notes:
# - The path in `ansible_ssh_private_key_file` can be an absolute path
#   (recommended), e.g. /home/edib/.ssh/mediaschool.
# - If you use an SSH agent or passwordless key, you can omit the
#   `ansible_ssh_private_key_file` option.
